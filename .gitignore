# dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# local environment & secrets (never commit real keys)
.env
*.local

# OS / editor
.DS_Store

# Claude Code — share team config (commands/skills/agents/settings),
# keep personal & local files (settings.local.json, launch.json) private
.claude/*
!.claude/commands/
!.claude/skills/
!.claude/agents/
!.claude/settings.json
!.claude/README.md

# generated artifacts — not part of the source (regenerate locally)
graphify-out/
bench/results/
.playwright-mcp/

# stray local screenshots
/final-cockpit.png
/verify-*.png

# user-supplied bench reference image (may be copyrighted / large — not committed)
bench/_ref.png

# Playwright e2e artifacts
test-results/
playwright-report/
